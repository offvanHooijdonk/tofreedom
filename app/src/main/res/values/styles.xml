<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="StartText">
        <item name="android:textSize">@dimen/start_text</item>
        <item name="android:padding">8dp</item>
        <item name="android:background">?android:attr/selectableItemBackground</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:clickable">true</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textColor">@color/md_white_1000</item>
    </style>

    <style name="StartPickedTime">
        <item name="android:textSize">@dimen/start_picked_time</item>
        <item name="android:layout_marginBottom">@dimen/activity_margin_v</item>
        <item name="android:textColor">@color/start_picked_date</item>
    </style>

    <style name="CountdownYearText">
        <item name="android:textSize">42sp</item>
        <item name="android:layout_marginEnd">8dp</item>
    </style>

    <style name="CountdownMonthDayText">
        <item name="android:textSize">32sp</item>
        <item name="android:layout_marginEnd">8dp</item>
    </style>

    <style name="CountdownTimeText">
        <item name="android:textSize">36sp</item>
        <item name="android:layout_marginEnd">4dp</item>
    </style>

</resources>
